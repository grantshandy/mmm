
pub fn turn_on() -> Result<(), (String, usize)> {
    println!("TURNED ON!!!");

    return Ok(());
}

pub fn turn_off() -> Result<(), (String, usize)> {
    println!("TURNED OFF!!!");

    return Ok(());
}